<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scrollview_desition"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#ededed"
    android:fillViewport="true"
    android:scrollbars="none" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical" >

        <include layout="@layout/header_desic" />

        <include layout="@layout/recly_view" />

        <include
            layout="@layout/other_xia_shang"
            android:visibility="gone" />

        <!--
        <LinearLayout
            android:id="@+id/llt_web_new"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
                android:visibility="gone"
            android:orientation="vertical" >

            <WebView
                android:id="@+id/webView_hgh_new"
                android:layout_width="match_parent"
                android:layout_height="match_parent" />
        </LinearLayout>
        -->

        <include layout="@layout/pub_lic_shouru_desic" />

    </LinearLayout>

</ScrollView>